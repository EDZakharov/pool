import React from "react";
import style from "./BlocksPageComponent.module.scss"
// import {NavLink} from "react-router-dom";

const Blocks = (props) => {
    return (
        <div className={style.blocks}>
            <div>123</div>
        </div>
    );
}

export default Blocks;
